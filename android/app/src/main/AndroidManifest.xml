<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="QuickIT"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
            FileProvider: required so share_plus can share temp image files
            without causing FileUriExposedException on Android 7+.
            The authority MUST match your applicationId (set in build.gradle.kts).
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
            ClipboardContentProvider: REQUIRED by super_clipboard package for
            image copy/paste to work on Android. It serves clipboard image data
            to receiving apps (WhatsApp, Gmail, Photos, etc.) via a content URI.
            Without this, "Copy Image" silently succeeds but paste produces nothing.
        -->
        <provider
            android:name="com.superlist.clipboard.ClipboardContentProvider"
            android:authorities="${applicationId}.ClipboardContentProvider"
            android:exported="true"
            android:grantUriPermissions="true">
        </provider>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Allow querying apps that can receive shared images -->
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="image/*"/>
        </intent>
    </queries>

    <!-- Camera permission (for scanner) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
        Storage permissions:
        • Android ≤ 9  (API 28-): WRITE_EXTERNAL_STORAGE needed for gallery save.
        • Android 10   (API 29): MediaStore handles saves; WRITE limited to maxSdkVersion.
        • Android 10-12 (API 29-32): READ_EXTERNAL_STORAGE for reading media.
        • Android 13+  (API 33+): Fine-grained READ_MEDIA_IMAGES replaces READ_EXTERNAL_STORAGE.
        The `gal` package uses MediaStore internally and requests the right
        permission at runtime, so the manifest just needs to declare them all.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Internet (optional, keep if you add any network features) -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
